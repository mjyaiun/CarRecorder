<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
   <!-- 背景  gradient是渐变,corners定义的是圆角 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="20dp" />

            <solid android:color="@color/color_deep_blue" />
        </shape>
    </item>
    <!-- 第二条进度条颜色 
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="20dp" />

                <gradient
                    android:angle="90.0"
                    android:centerColor="@color/color_deep_blue"
                    android:centerY="0.45"
                    android:endColor="@color/color_deep_blue"
                    android:startColor="@color/color_deep_blue" />
            </shape>
        </clip>
    </item>-->
    <!-- 进度条 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="20dp" />

                <solid android:color="@color/color_green_deep" />
            </shape>
        </clip>
    </item>
    

</layer-list>
